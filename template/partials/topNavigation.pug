nav.navbar.navbar-default(role="navigation")
  ul.nav.navbar-nav
    li
      a(href="/") Главная
    if user
      li
        a(href="/chat") Чат
  ul.nav.navbar-nav.navbar-right
    if user
      li
        a(href="" onclick="$(document.body).append($('<form method=POST action=/logout>'));$('form').submit();return false") Выйти
    else
      li
        a(href="/login") Войти
